name: country_calling_code_picker
publish_to: none
description: Flexible Country picker for getting Country code and Calling code.
version: 2.0.1
homepage: https://github.com/dev-naiksan/country-calling-code-picker.git

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_sim_country_code:
    git:
      url: https://github.com/knottx/flutter_sim_country_code.git


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - flags/
    - raw/
